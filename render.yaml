services:
  - type: web
    name: currency-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: REGION
        value: AR
    healthCheckPath: /health
    autoDeploy: true
